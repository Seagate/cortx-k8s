apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.cortxdataprov.localpathpvc.name }}
  namespace: {{ .Values.namespace }}
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: {{ .Values.cortxdataprov.localpathpvc.requeststoragesize }}
