apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.cortxprov.service.name }}
  namespace: {{ .Values.namespace }}
spec:
  type: ClusterIP
  selector:
    app: {{ .Values.cortxprov.name }}
  ports:
    - name: cortx-prov-tcp
      protocol: TCP
      port: 5050
      targetPort: 5050